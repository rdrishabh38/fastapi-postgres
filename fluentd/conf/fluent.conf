<source>
  @type forward
  port 24224
  bind "0.0.0.0"
</source>

<source>
  @type monitor_agent
  port 24220
  bind "0.0.0.0"
</source>

<match docker.**>
  @type elasticsearch
  host 172.28.0.5
  port 9200
  logstash_format true
  logstash_prefix docker-logs
  flush_interval 5s
</match>
